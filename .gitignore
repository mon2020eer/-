__pycache__/
*.py[cod]
.env
venv/
.venv/
.pytest_cache/

# --- مخرجات البناء ---
# المسارات **بلا بادئة مجلد**: كان مكتوباً `car_rental/build/dist/` حين كان
# التطبيق في مجلد فرعي، فلمّا نُقل إلى الجذر ماتت القاعدة بصمت وصار ملف
# ويندوز التنفيذي (عشرات الميغابايتات) مرشَّحاً للإيداع.
build/dist/
build/work/
build/installer/

# --- بيانات التشغيل ---
*.db
*.db-wal
*.db-shm
*.db.gz
credentials.json
token.json

# --- أسرار الترخيص — لا تُودَع في المستودع أبداً ---
*.license_private.key
.car_rental_license_private.key
# سجلّ المفاتيح المُصدَرة: فيه مفاتيح العملاء وأسماء مكاتبهم وتواريخ اشتراكهم
tools/issued_licenses.jsonl
